// 错误情况测试用例 - 用于测试词法分析器在遇到非法输入时的行为

// 1. 不匹配的注释
/* 这个多行注释没有正确结束
// 文件结束前没有关闭注释

// 2. 不完整的字符串
string unclosed_string = "这个字符串没有闭合

// 3. 非法字符测试
int @at_sign = 1;
int #hash = 2;
int $dollar = 3;
int %percent = 4;

// 4. 非法数字格式
int invalid_number = 12.34;   // 浮点数（如果语言不支持）
int hex_number = 0xFF;        // 十六进制数（如果语言不支持）
int octal_number = 0755;      // 八进制数（如果语言不支持）

// 5. 非法运算符组合
int a = 5;
int b = a === 10;    // 连续的等号
int c = a !== 10;    // 非法的不等号
int d = a ==> 10;    // 箭头运算符（如果不支持）
int e = a ++ 1;      // 错误的递增运算符位置
int f = a .. b;      // 双点运算符（如果不支持）

// 6. 非法关键字使用
int int = 5;         // 关键字不能作为标识符
bool bool = true;    // 关键字不能作为标识符

// 7. 空字符和控制字符
int test_null = 0;   // 测试嵌入的null字符
int test_control = 1; // 测试其他控制字符

// 8. Unicode和特殊字符
int 测试中文标识符 = 1;  // 中文标识符（如果语言不支持）
int emoji = 2;       // Emoji变量名😀（如果不支持）

// 9. 极端嵌套
void deeply_nested() {
    if (true) {
        if (true) {
            if (true) {
                if (true) {
                    if (true) {
                        if (true) {
                            if (true) {
                                if (true) {
                                    if (true) {
                                        if (true) {
                                            // 深度嵌套的if语句
                                            int deep = 0;
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

// 10. 混合错误情况
void mixed_errors() {
    @ illegal_char_before = 1;
    int unclosed_string = "未闭合的字符串
    int@invalid = 2;  // 合法标识符后跟非法字符
    int multiple=======equals = 3;
    int invalid_suffix123abc = 4;
}

// 11. 文件末尾边界情况
void eof_boundary_cases() {
    int normal = 1;  // 正常语句，但文件立即结束，无换行符
}